# TV Guide - Deploy Now

## Files (4 total):

1. channels_backend.py  → islandforge/routes/channels.py
2. channels.html        → islandforge/templates/channels.html  
3. channels.css         → islandforge/static/css/channels.css
4. channels.js          → islandforge/static/js/channels.js

---

## Add to server.py:

```python
# Import
from routes.channels import channels_bp

# Register
app.register_blueprint(channels_bp)

# Route
@app.route('/channels')
def channels_page():
    return render_template('channels.html')
```

---

## Configure Channels:

Edit `islandforge/routes/channels.py` line 18:

```python
"ttf-live": {
    "stream_id": "YOUR_TWITCH_USERNAME",  # ← Change this
    "type": "twitch",
    "live": True
}
```

---

## Deploy:

```powershell
git add -A
git commit -m "Add TV Guide channels"
git push origin main
```

```powershell
ssh -i "C:\Users\xinc\Downloads\ssh-key-2.key" ubuntu@129.80.222.152
```

```bash
cd ~/ver-perlinforge
git pull origin main
sudo systemctl restart islandforge
sudo systemctl status islandforge
exit
```

Visit: https://triptokforge.org/channels

DONE! 🎬
